# PostgreSQL (ajusta usuario, password, host, puerto y nombre de DB)
DATABASE_URL="postgresql://postgres:TU_PASSWORD@localhost:5432/geoasistencia_db?schema=public"

# JWT
JWT_SECRET="cambia_esto_por_una_clave_larga_y_unica"
JWT_EXPIRES_IN="8h"

# Server
PORT=4000
CORS_ORIGIN="http://localhost:5173"
